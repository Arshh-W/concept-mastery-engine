import React from "react";

const GoalPanel = () => {
    return (
        <div className="goal-panel">
            <h2>Goal Panel</h2>
            <p>This panel displays the current goals and progress.</p>
        </div>
    );
}